#ifndef CHANGE_OFFSET_H
#define CHANGE_OFFSET_H

#include <stdint.h>

//  Change the value of the scrolled screen offset and repaint the screen
void change_offset(int16_t n);

#endif//!CHANGE_OFFSET_H
